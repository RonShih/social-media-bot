# Optional. Copy to config/secrets.yaml (gitignored) and fill if needed.
# Skills must NEVER read this file directly — pull from env vars wired
# through .claude/settings.json or shell profile.

# Image generation provider (image-generator skill).
openai_api_key: ""

# Reserved for future use.
anthropic_api_key: ""
